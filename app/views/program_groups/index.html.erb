<ul>
<%= "Total program group Number = " + @program_groups.size.to_s %>
<br>
<% @program_groups.each do |group| %>
  <li>
    <%= link_to group.name, program_group_path(group) %>
    <%= " (" + group.tv_programs.size().to_s + ")" %>
  </li>
<% end %>
</ul>


